const text = document.createElement("textarea");
text.id = "te";
document.body.appendChild(text);
const button = document.createElement("button");
button.id = "Go";
button.innerText = "Send"
button.onclick="sort()"
document.body.appendChild(button);
function sort() {
  if (database != document.getElementById("te"))
  const doh = document.createElement("p");
doh.innerText = "Hmm, I don't know that one, adding to database";
document.body.appendChild(doh);
} else {
 const doh = document.createElement("p");
doh.innerText = database;
document.body.appendChild(doh);
}
